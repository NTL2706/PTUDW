<div class="content-wrapper">
    <!-- Content -->

    <div class="container-xxl flex-grow-1 container-p-y">
        <h4 class="py-3 mb-4">
            <span class="text-muted fw-light">View/</span> District
        </h4>

        <!-- Basic Bootstrap Table -->
        <div class="demo-inline-spacing" style="text-align: right;">
            <a href="/cb/create-district">
                <button type="button" class="btn btn-outline-primary">
                    Create
                </button>
            </a>
        </div>
        <br>
        <div class="card">
            <div class="table-responsive text-nowrap">
                <table class="table">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody class="table-border-bottom-0">
                        {{#each district}}
                        <tr>

                            <td>
                                <span class="fw-medium">{{this.name}}</span>
                            </td>

                            <td>
                                <div class="dropdown">
                                    <button type="button" class="btn p-0 dropdown-toggle hide-arrow"
                                        data-bs-toggle="dropdown">
                                        <i class="mdi mdi-dots-vertical"></i>
                                    </button>

                                    <div class="dropdown-menu">
                                        <a class="dropdown-item" href="/cb/viewWard?id={{this._id}}"><i
                                                class="mdi mdi-view-array-outline me-1"></i>
                                            View ward</a>
                                        <a class="dropdown-item" href="/cb/editDistrict?id={{this._id}}"><i
                                                class="mdi mdi-pencil-outline me-1"></i>
                                            Edit</a>
                                        <a class="dropdown-item" href="/cb/deleteDistrict?id={{this._id}}"><i
                                                class="mdi mdi-trash-can-outline me-1"></i>
                                            Delete</a>
                                    </div>

                                </div>
                            </td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
                <hr class="my-3" />
            </div>
        </div>